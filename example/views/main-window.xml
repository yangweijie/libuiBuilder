<window title="{{app_name}} v{{version}}" size="1000,700">
    <vbox>
        <!-- 菜单栏 -->
        <menu>
            <item text="文件">
                <submenu>
                    <item text="新建" onclick="fileNew"/>
                    <item text="打开" onclick="fileOpen"/>
                    <separator/>
                    <item text="退出" onclick="quit"/>
                </submenu>
            </item>
            <item text="帮助">
                <submenu>
                    <item text="关于" onclick="about"/>
                </submenu>
            </item>
        </menu>

        <!-- 主内容 -->
        @include('components.toolbar')

        <hbox stretchy="true">
            @include('components.sidebar')
            <separator orientation="vertical"/>
            @include('components.content-area')
        </hbox>

        @include('components.status-bar')
    </vbox>
</window>